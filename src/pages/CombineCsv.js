import React, { useState, useContext, useEffect, useMemo } from 'react';
import Combine from '../component/Combine';

export default function CombineCsv() {
    return (
        <div>
            <div className="button-container">
                <Combine />
            </div>
        </div>
    )
}
